{% extends 'base.html.twig' %}

{% block title %}Hello AdminCrudSliderController!{% endblock %}

{% block body %}

<div>

<h1 class="text-center">Admin Produits</h1>

	{# <a href="" class="btn btn-success my-3">Ajouter un slider</a> #}

	<table class="table table-bordered text-center">
		<thead>
			<tr>
				<th>ordre</th>
				<th>photo</th>
                <th>date</th>
				
				<th></th>
				<th></th>
				
			</tr>
		</thead>
		<tbody>
			{% for item in photo %}
				<tr>
					<td>{{ item.ordre }}</td>
				
					<td><img src="{{asset('images/'~ item.photo)}}" alt="Photo " style="width: 150px;"></td>
					
					<td>{{ item.createdAt|date('d/m/Y à H:i:s') }}</td>
					<td>
						<a href="{{path('admin_crud_slider_edit',{'id':item.id})}}" class="text-dark">
							<i class="fas fa-edit fa-lg"></i>
						</a>
                    </td>
					<td>
						 <a onclick="return confirm('Voulez-vous supprimer ce produit ?')" href="{{path('admin_crud_slider_delete',{'id':item.id} )}}"<class="text-dark">
							<i class="fas fa-trash-alt fa-lg"></i>
						</a> 
					</td>
				</tr>
			{% endfor %}
		</tbody>

	</table>


</div><br><br>
 <h1 class="text-center">Ajouter un slider</h1>

<div class="container col-md-6">
{{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}

	{{ form_row(form.ordre) }}
	
	{{ form_row(form.file ) }}

	
	

	<button class="btn btn-primary my-2" type="submit">Valider</button>

{{ form_end(form) }}

</div>
{% endblock %}
